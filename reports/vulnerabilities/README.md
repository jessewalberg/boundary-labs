# Vulnerability Reports

No confirmed vulnerabilities were promoted from the current MVP seed run. The latest run in `evals/results/latest.json` contains four passing seed cases.

When the Judge Agent confirms a `fail` or high-confidence `partial`, the Documentation Agent should create reports here using the template in `ARCHITECTURE.md`.
